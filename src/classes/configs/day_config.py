class DayConfig:
    def __init__(self, num_banks, bank_types, starting_balance, matching_window):
        self.num_banks = num_banks
        self.bank_types = bank_types
        self.starting_balance = starting_balance
        self.matching_window = matching_window
